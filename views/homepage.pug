html
  head
    title Books
  body
    if books.length == 0 
        h1 The library is empty
    else 
        h1 Books
        ul
        each book in books   
            li(style ='list-style-type: none;')
                p= book.name      