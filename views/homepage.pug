html
  head
    title Books
  body
    if allBooks.length == 0 
        h1 The library is empty
    else 
        h1 Books
        ul
        each book in allBooks   
            li(style ='list-style-type: none;')
                p= book.name      